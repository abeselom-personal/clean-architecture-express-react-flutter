import 'package:flutter/material.dart';

class AppColors {
  /// White and black colors
  static const Color white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const dark = Color(0xFF1A1A1A);

  /// Main color of the app
  static const Color primary = Color(0xFFB119FB);
  static const Color secondary = Color(0xFFE1992B);
  static const Color tertiary = Color(0xFF4993FF);

  /// On Main color of the app (Light mode)
  static const Color onPrimaryLight = Color(0xFFFFFFFF);
  static const Color onSecondaryLight = Color(0xFFFFFFFF);
  static const Color onTertiaryLight = Color(0xFFFFFFFF);

  /// On Main color of the app (Dark mode)
  static const Color onPrimaryDark = Color(0xFFFFFFFF);
  static const Color onSecondaryDark = Color(0xFFFFFFFF);
  static const Color onTertiaryDark = Color(0xFFFFFFFF);

  /// Gray colors
  static Color gray = Colors.grey.shade500;
  static Color lightGray = Colors.grey.shade300;
  static Color darkGray = Colors.grey.shade800;

  /// Transparent color
  static const Color transparent = Colors.transparent;

  /// Success and error colors
  static const Color successColor = Color(0xFF0ED114);
  static const Color errorColor = Color(0xFFFF6262);
}
